<%= form_for(@pedometer_reading) do |f| %>
  <% if @pedometer_reading.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pedometer_reading.errors.count, "error") %> prohibited this pedometer_reading from being saved:</h2>

      <ul>
      <% @pedometer_reading.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :steps %><br>
    <%= f.number_field :steps %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
